name: Deploy Content to mssentinelworkspace [58bea6b8-c8e1-4e0f-989d-8617a73d74f8]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-58bea6b8-c8e1-4e0f-989d-8617a73d74f8.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ master ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-58bea6b8-c8e1-4e0f-989d-8617a73d74f8.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'kevin1'
      workspaceName: 'mssentinelworkspace'
      workspaceId: '5d5955f2-0d15-4744-993a-5641ff9d3a4c'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'Watchlist,Workbook,Parser,Playbook,HuntingQuery,AutomationRule,AnalyticsRule'
      branch: 'master'
      sourceControlId: '58bea6b8-c8e1-4e0f-989d-8617a73d74f8'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_58bea6b8c8e14e0f989d8617a73d74f8 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-58bea6b8-c8e1-4e0f-989d-8617a73d74f8.ps1